namespace WinUX.Maths
{
    /// <summary>
    /// Defines the enumeration values for standard units of measure.
    /// </summary>
    public enum UnitOfMeasure
    {
        /// <summary>
        /// Metric unit (cm, ml, etc.)
        /// </summary>
        Metric,

        /// <summary>
        /// Imperial unit (in, lb, etc.)
        /// </summary>
        Imperial
    }
}